mode: ContinuousDeployment
branches:
  main:
    regex: ^main$
    increment: None
    track-merge-target: false
    is-release-branch: true
  develop:
    regex: ^dev(elop)?$
    increment: None
    is-source-branch-for: ['main']
ignore:
  sha: []
